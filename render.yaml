services:
  - type: web
    name: hotel-report-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: GEMINI_API_KEY
        sync: false
    healthCheckPath: /health
